import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {Screen} from '@/components';

const AboutScreen = () => {
  return <Screen title="About"></Screen>;
};

export default AboutScreen;

const styles = StyleSheet.create({});
